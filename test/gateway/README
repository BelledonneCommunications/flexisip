Create tap:
sudo tunctl
sudo ifconfig tap0 10.0.0.1

Start two flexisip:
flexisip --configfile flexisip2.conf --debug
flexisip --configfile flexisip.conf --debug

Start sipp:
sipp -sf invite.xml 127.0.0.1 -i 127.0.0.1 -l 1 -m 1
